.module
  .module-header Access Denied
  .module-content
    h1 Access Denied
    p You do not have sufficient permissions to access the requested page.
    - if (redirect)
      - redirect.time = redirect.time || 3
      p 
        You are being redirected
        a(href=redirect.href) here
        in #{redirect.time} seconds.
      :javascript
        | $(document).ready(function(){
        |     setTimeout(function(){
        |         window.location = '#{redirect.href}';
        |     }, #{redirect.time * 1000});
        | });
