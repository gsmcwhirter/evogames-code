Dear Owners and Application Admins of [<%= group.name %> (<%= group.code %>)](/group/<%= group.code %>),

The player [<%= alias %>@<%= handle %>](/player/<%= alias %>@<%= handle %>) has requested to join your group.
To approve or deny the request, please visit the [Membership Requests page](/group/<%= group.code %>/controls/requests).

\- <%= system.name %>