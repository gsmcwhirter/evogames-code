Dear Owners and Event Admins of [<%= registration.name_or_alias %> (<%= registration.code_or_handle %>)](/group/<%= registration.code_or_handle %>),

Your group's registration request for
[<%= event.name %> (<%= event.gamecode.toUpperCase() %> <%= event.gametype.name.toUpperCase() %>)](/game/<%= event.gamecode %>/event/<%= event.slug %>)
has been approved.

\- <%= system.name %>