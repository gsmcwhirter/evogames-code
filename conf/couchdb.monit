#!monit
set logfile /var/log/monit.log

check host couchdb with address 127.0.0.1
    start program = "/sbin/start couchdb"
    stop program = "/sbin/stop couchdb"
    if failed port 5984 protocol HTTP
        request /
        with timeout 10 seconds
        then restart
